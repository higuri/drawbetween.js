{
  "name": "cjs",
  "version": "1.0.0",
  "main": "",
  "license": "MIT",
  "scripts": {
    "build": "yarn link drawbetween && browserify main.js -o bundle.js",
    "start": "opn index.html",
    "clean": "yarn unlink drawbetween && rimraf bundle.js"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "opn-cli": "^4.0.0",
    "rimraf": "^2.6.3"
  }
}
